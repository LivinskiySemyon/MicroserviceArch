# === Database Configuration ===
# Main Database (для articles, comments)
DB_MAIN_USER=app
DB_MAIN_PASSWORD=app
DB_MAIN_NAME=app

# Users Database
DB_USERS_USER=app
DB_USERS_PASSWORD=app
DB_USERS_NAME=users_db

# === JWT Configuration ===
# Используется ОБА сервисами (backend и users-api)
JWT_SECRET=your-secret-key-keep-it-safe-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRES_MIN=30
